package httpconst

const (
	// ID defines the name of the (optional) URL query string key containing
	// the ID of the requested transfer.
	ID = "id"

	// Rule defines the name of the (optional) URL query string key containing
	// the name of the transfer rule.
	Rule = "rule"
)
